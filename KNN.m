function [KNN_class]  = KNN(data)
no_of_classes = size(data,1);
samplesPclass = size(data,2);
xl = data(:,1, :);

for k=3:no_of_classes
    for k=3:samplesPclass
       %vectorize 
    
    end
    
end